// https://projecteuler.net/problem=8

#include <math.h>
#include <stdio.h>

int main() {

    FILE *fp;
    char buff[255];

    fp = fopen("number.txt", "r");

    char z = (buff, 3, (FILE*)fp);
    printf("2: %s\n", buff );
   
    return 0;
}